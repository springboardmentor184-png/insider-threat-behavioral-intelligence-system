import { useState } from "react";
import { useRouter } from "next/router";
import Link from "next/link";
import { Lock, ShieldCheck } from "lucide-react";
const API_BASE = process.env.NEXT_PUBLIC_API_BASE || "http://localhost:8000";
export default function ResetPassword() {
  const router = useRouter(); const [password, setPassword] = useState(""); const [confirmation, setConfirmation] = useState(""); const [error, setError] = useState(""); const [success, setSuccess] = useState(""); const [loading, setLoading] = useState(false);
  const submit = async (event) => { event.preventDefault(); setError(""); if (!router.query.token) return setError("A reset token is required."); if (password !== confirmation) return setError("Passwords do not match."); setLoading(true); try { const response = await fetch(`${API_BASE}/password-reset/confirm`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ token: router.query.token, password }) }); const data = await response.json(); if (!response.ok) throw new Error(data.detail || "Password reset failed"); setSuccess(data.message); setTimeout(() => router.push("/login"), 1400); } catch (err) { setError(err.message); } finally { setLoading(false); } };
  return <main className="min-h-screen flex items-center justify-center p-6" style={{ background: "var(--color-bg)" }}><form onSubmit={submit} className="card w-full max-w-md p-7"><div className="flex items-center gap-2 mb-6"><ShieldCheck size={22} style={{ color: "var(--color-primary)" }} /><div><h1 className="text-xl font-bold">Choose a new password</h1><p className="text-sm" style={{ color: "var(--color-text-muted)" }}>Your reset token is valid for 30 minutes.</p></div></div>{error && <p className="text-sm p-3 rounded mb-4" style={{ background: "var(--sev-critical-bg)", color: "var(--sev-critical)" }}>{error}</p>}{success && <p className="text-sm p-3 rounded mb-4" style={{ background: "var(--color-primary-soft)", color: "var(--color-primary-dark)" }}>{success}</p>}<label className="block text-xs font-medium mb-1.5">New password</label><div className="relative mb-4"><Lock size={16} className="absolute left-3 top-1/2 -translate-y-1/2" style={{ color: "var(--color-text-faint)" }} /><input className="input-field pl-9" type="password" minLength="8" value={password} onChange={(event) => setPassword(event.target.value)} required /></div><label className="block text-xs font-medium mb-1.5">Confirm new password</label><input className="input-field mb-5" type="password" minLength="8" value={confirmation} onChange={(event) => setConfirmation(event.target.value)} required /><button className="btn-primary w-full py-2.5 text-sm" disabled={loading || Boolean(success)}>{loading ? "Resetting…" : "Set new password"}</button><p className="text-xs text-center mt-5"><Link href="/login" style={{ color: "var(--color-primary)" }}>Back to sign in</Link></p></form></main>;
}
